import { Dimensions } from "react-native"

export const colors = {
    primary: 'blue',
    accent: 'orange',
    error: '#F00',
    black: '#000000',
    grey: '#CFCFCF',
    white: '#FFFFFF'
}

export const {width, height} = Dimensions.get('screen');
